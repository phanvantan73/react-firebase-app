import styled from 'styled-components';
import { Layout } from 'antd';

const { Content } = Layout;

export const ContentWrapper = styled(Content)`
  padding: 0 50px;
`;

export const SiteContentWrapper = styled.div`
  min-height: 280px;
  padding: 24px;
  background: #fff;
`;
